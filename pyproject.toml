[project]
name = "ecuroad-api"
version = "0.1.0"
requires-python = ">=3.11"
description = "EcuRoad backend API — Colombian vocational orientation platform"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
